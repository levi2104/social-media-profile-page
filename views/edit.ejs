<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
  <!-- main div -->
  <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
    <a href="/logout" class="px-5 py-2 bg-red-500 rounded-md cursor-pointer">Logout</a>
    <h3 class="mb-5 text-center">Edit your post</h3>
    <form action="/update/<%= post._id %>" method="post" class="flex flex-col gap-3 justify-center items-center">
      <textarea class="p-3 w-1/3 rounded-md bg-transparent border-2 border-zinc-600 outline-none resize-none h-28"
        placeholder="What's on your mind today?" name="content"><%= post.content %></textarea>
      <input type="submit" value="Edit Post" class="mt-3 px-3 py-2 bg-yellow-500 rounded-md text-black cursor-pointer">
    </form>
  </div>
</body>

</html>