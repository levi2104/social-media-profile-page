<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
  <!-- main div -->
  <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
    <a href="/logout" class="px-5 py-2 bg-red-500 rounded-md cursor-pointer">Logout</a>
    <div class="flex gap-3 justify-center">
      <div class="w-10 h-10">
        <img src="/images/uploads/<%= user.profilepic %>" class="w-full h-full object-cover rounded-full">
      </div>
      <h1 class="text-3xl text-center mb-10">Hello, <span class="font-bold"><%= user.name %></span></h1>
    </div>
    <h3 class="mb-5 text-center">Create a new post</h3>
    <form action="/post" method="post" class="flex flex-col gap-3 justify-center items-center">
      <textarea class="p-3 w-1/3 rounded-md bg-transparent border-2 border-zinc-600 outline-none resize-none h-28" placeholder="What's on your mind today?" name="content"></textarea>
      <input type="submit" value="Create Post" class="mt-3 px-3 py-2 bg-green-500 rounded-md text-black cursor-pointer">
    </form>

    <div class="mt-20">
      <h3 class="text-zinc-400 mb-4 text-center">Your Posts</h3>
      <div class="flex flex-wrap gap-3">
        <% user.posts.reverse().forEach(post => { %>
          <div class="w-1/4 p-4 mb-4 border-[1px] border-zinc-600 bg-zinc-800 rounded-md">
            <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
            <p class="text-sm"><%= post.content %></p>
            <small class="inline-block mt-2"><%= post.likes.length %> likes</small>
            <div class="flex mt-5 gap-5">
              <a href="/like/<%= post._id %>" class="text-blue-500">
                <%= post.likes.indexOf(user._id) === -1 ? 'Like' : 'Unlike' %>
              </a>
              <a href="/edit/<%= post._id %>" class="text-zinc-400">Edit</a>
            </div>
          </div>
        <% }) %>
      </div>
    </div>
  </div>
</body>

</html>